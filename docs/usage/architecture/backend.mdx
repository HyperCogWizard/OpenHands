---
title: Backend Architecture
---

<div style={{ textAlign: 'center' }}>
  <img src="https://github.com/All-Hands-AI/OpenHands/assets/16201837/97d747e3-29d8-4ccb-8d34-6ad1adb17f38" alt="OpenHands System Architecture Diagram Jul 4 2024" />
  <p><em>OpenHands System Architecture Diagram (July 4, 2024)</em></p>
</div>

This is a high-level overview of the system architecture. The system is divided into two main components: the frontend and the backend. The frontend is responsible for handling user interactions and displaying the results. The backend is responsible for handling the business logic and executing the agents.

# Frontend architecture

![system_architecture.svg](/static/img/system_architecture.svg)

This Overview is simplified to show the main components and their interactions. For a more detailed view of the backend architecture, see the Backend Architecture section below.

# Backend Architecture

_**Disclaimer**: The backend architecture is a work in progress and is subject to change. The following diagrams show the current architecture of the backend with comprehensive Mermaid visualizations._

## Backend Component Architecture

This diagram shows the detailed backend component structure and their interactions:

```mermaid
graph TB
    subgraph "Server Layer"
        direction TB
        FastAPI[FastAPI Server]
        FastAPI --> Routes[API Routes]
        FastAPI --> Middleware[Middleware Stack]
        FastAPI --> Dependencies[Dependency Injection]
        
        Routes --> SessionRoutes[Session Management]
        Routes --> AgentRoutes[Agent Operations]
        Routes --> FileRoutes[File Operations]
        Routes --> ConfigRoutes[Configuration]
    end
    
    subgraph "Session Management"
        direction TB
        ConversationManager[Conversation Manager]
        ConversationManager --> SessionStore[Session Store]
        ConversationManager --> SessionCleanup[Session Cleanup]
        
        SessionStore --> Session[Session Instance]
        Session --> EventStream[EventStream]
        Session --> AgentController[Agent Controller]
        Session --> Runtime[Runtime Instance]
    end
    
    subgraph "Agent Processing Core"
        direction TB
        AgentController --> Agent[Agent Instance]
        AgentController --> State[Agent State]
        AgentController --> StuckDetector[Stuck Detection]
        
        Agent --> LLMClient[LLM Client]
        Agent --> ActionParser[Action Parser]
        Agent --> PromptBuilder[Prompt Builder]
        
        State --> EventHistory[Event History]
        State --> TaskState[Task State]
        State --> MemoryState[Memory State]
    end
    
    subgraph "Event Processing System"
        direction TB
        EventStream --> EventStore[Event Store]
        EventStream --> EventQueue[Event Queue]
        EventStream --> Subscribers[Event Subscribers]
        
        EventStore --> FileStore[File Storage]
        EventQueue --> AsyncProcessor[Async Processor]
        
        Subscribers --> MemorySubscriber[Memory]
        Subscribers --> SecuritySubscriber[Security]
        Subscribers --> RuntimeSubscriber[Runtime]
    end
    
    subgraph "Runtime Execution"
        direction TB
        Runtime --> RuntimeSelector[Runtime Selector]
        RuntimeSelector --> DockerRuntime[Docker Runtime]
        RuntimeSelector --> E2BRuntime[E2B Runtime]
        RuntimeSelector --> LocalRuntime[Local Runtime]
        
        DockerRuntime --> ActionExecutor[Action Executor]
        E2BRuntime --> ActionExecutor
        LocalRuntime --> ActionExecutor
        
        ActionExecutor --> CommandRunner[Command Runner]
        ActionExecutor --> FileManager[File Manager]
        ActionExecutor --> BrowserController[Browser Controller]
    end
    
    %% Cross-layer connections
    SessionRoutes --> ConversationManager
    AgentRoutes --> AgentController
    FileRoutes --> Runtime
    
    EventStream --> MemorySubscriber
    EventStream --> SecuritySubscriber
    EventStream --> RuntimeSubscriber
    
    ActionExecutor --> EventStream
    LLMClient --> EventStream
    
    classDef server fill:#e3f2fd
    classDef session fill:#e8f5e8
    classDef agent fill:#fff3e0
    classDef event fill:#f3e5f5
    classDef runtime fill:#ffebee
    
    class FastAPI,Routes,Middleware,Dependencies,SessionRoutes,AgentRoutes,FileRoutes,ConfigRoutes server
    class ConversationManager,SessionStore,SessionCleanup,Session,EventStream session
    class AgentController,Agent,State,StuckDetector,LLMClient,ActionParser,PromptBuilder,EventHistory,TaskState,MemoryState agent
    class EventStore,EventQueue,Subscribers,FileStore,AsyncProcessor,MemorySubscriber,SecuritySubscriber,RuntimeSubscriber event
    class Runtime,RuntimeSelector,DockerRuntime,E2BRuntime,LocalRuntime,ActionExecutor,CommandRunner,FileManager,BrowserController runtime
```

## Backend Data Flow Architecture

This diagram illustrates the data flow patterns and processing pipelines:

```mermaid
flowchart TD
    subgraph "Request Processing Flow"
        direction TB
        ClientRequest[Client Request] --> RequestValidation[Request Validation]
        RequestValidation --> Authentication[Authentication]
        Authentication --> SessionLookup[Session Lookup]
        SessionLookup --> RequestRouting[Request Routing]
    end
    
    subgraph "Agent Processing Pipeline"
        direction TB
        TaskReceived[Task Received] --> StateAnalysis[State Analysis]
        StateAnalysis --> ContextRetrieval[Context Retrieval]
        ContextRetrieval --> PromptGeneration[Prompt Generation]
        PromptGeneration --> LLMInference[LLM Inference]
        LLMInference --> ResponseParsing[Response Parsing]
        ResponseParsing --> ActionGeneration[Action Generation]
    end
    
    subgraph "Execution Pipeline"
        direction TB
        ActionValidation[Action Validation] --> SecurityCheck[Security Check]
        SecurityCheck --> RuntimeDispatch[Runtime Dispatch]
        RuntimeDispatch --> CommandExecution[Command Execution]
        CommandExecution --> ResultCapture[Result Capture]
        ResultCapture --> ObservationCreation[Observation Creation]
    end
    
    subgraph "Event Distribution Flow"
        direction TB
        EventCreation[Event Creation] --> EventSerialization[Event Serialization]
        EventSerialization --> EventQueuing[Event Queuing]
        EventQueuing --> SubscriberNotification[Subscriber Notification]
        SubscriberNotification --> EventPersistence[Event Persistence]
    end
    
    subgraph "Response Flow"
        direction TB
        ResponseAggregation[Response Aggregation] --> ResponseFormatting[Response Formatting]
        ResponseFormatting --> ClientDelivery[Client Delivery]
        ClientDelivery --> SessionUpdate[Session Update]
    end
    
    %% Flow connections
    RequestRouting --> TaskReceived
    ActionGeneration --> ActionValidation
    ObservationCreation --> EventCreation
    EventPersistence --> ResponseAggregation
    
    %% Feedback loops
    SessionUpdate -.-> StateAnalysis
    ResultCapture -.-> ContextRetrieval
    
    classDef request fill:#e3f2fd
    classDef agent fill:#e8f5e8
    classDef execution fill:#fff3e0
    classDef event fill:#f3e5f5
    classDef response fill:#ffebee
    
    class ClientRequest,RequestValidation,Authentication,SessionLookup,RequestRouting request
    class TaskReceived,StateAnalysis,ContextRetrieval,PromptGeneration,LLMInference,ResponseParsing,ActionGeneration agent
    class ActionValidation,SecurityCheck,RuntimeDispatch,CommandExecution,ResultCapture,ObservationCreation execution
    class EventCreation,EventSerialization,EventQueuing,SubscriberNotification,EventPersistence event
    class ResponseAggregation,ResponseFormatting,ClientDelivery,SessionUpdate response
```

## Legacy Architecture Reference

For comparison with the legacy PlantUML diagrams, the original backend architecture diagram is available at:

![backend_architecture.svg](/static/img/backend_architecture.svg)

<details>
  <summary>Updating this Diagram</summary>
  <div>
    The generation of the backend architecture diagram is partially automated.
    The diagram is generated from the type hints in the code using the py2puml
    tool. The diagram is then manually reviewed, adjusted and exported to PNG
    and SVG.

    ## Prerequisites

    - Running python environment in which openhands is executable
    (according to the instructions in the README.md file in the root of the repository)
    - [py2puml](https://github.com/lucsorel/py2puml) installed

## Steps

1.  Autogenerate the diagram by running the following command from the root of the repository:
    `py2puml openhands openhands > docs/architecture/backend_architecture.puml`

2.  Open the generated file in a PlantUML editor, e.g. Visual Studio Code with the PlantUML extension or [PlantText](https://www.planttext.com/)

3.  Review the generated PUML and make all necessary adjustments to the diagram (add missing parts, fix mistakes, improve positioning).
    _py2puml creates the diagram based on the type hints in the code, so missing or incorrect type hints may result in an incomplete or incorrect diagram._

4.  Review the diff between the new and the previous diagram and manually check if the changes are correct.
    _Make sure not to remove parts that were manually added to the diagram in the past and are still relevant._

5.  Add the commit hash of the commit that was used to generate the diagram to the diagram footer.

6.  Export the diagram as PNG and SVG files and replace the existing diagrams in the `docs/architecture` directory. This can be done with (e.g. [PlantText](https://www.planttext.com/))

  </div>
</details>
